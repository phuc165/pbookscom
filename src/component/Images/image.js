export const LogoIcon = ({
    width = '100%',
    height = 'fit-content',
    className,
}) => (
    <img
        className={className}
        width={width}
        height={height}
        src="images/header/logo.png"
        alt="logo"
    />
);
